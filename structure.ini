sentinelnode/
├── docker-compose.yml
├── .env.example
│
├── node-api/
│   ├── Dockerfile
│   ├── .dockerignore
│   ├── package.json
│   ├── tsconfig.json
│   ├── src/
│   │   ├── index.ts                  ← entry point, boots Hono
│   │   ├── shared/
│   │   │   ├── types.ts              ← all shared TS interfaces
│   │   │   ├── middleware/
│   │   │   │   └── auth.ts           ← JWT verification middleware
│   │   │   └── utils/
│   │   │       └── ipc.ts            ← child_process.spawn wrapper
│   │   └── modules/
│   │       ├── auth/
│   │       │   └── auth.routes.ts    ← /login, /logout, /refresh
│   │       ├── scanner/
│   │       │   └── scanner.routes.ts ← /scan/secrets, /scan/status
│   │       └── network/
│   │           └── network.routes.ts ← /audit/headers
│   └── data/                         ← SQLite volume mount point
│
└── rust-core/
    ├── Dockerfile
    ├── .dockerignore
    ├── Cargo.toml
    └── src/
        ├── main.rs                   ← reads stdin, dispatches job, writes stdout
        ├── scanner.rs                ← recursive fs walk + regex
        └── types.rs                  ← Job and Finding structs (serde)